// This file has been intentionally emptied — the canonical
// ResendOptions, IEmailSender, and ResendEmailSender now live in
// EmailSender.cs (post-merge). Safe to delete in git:
//
//     git rm api/Infrastructure/ResendEmailSender.cs
//
// Kept as a stub so the file still exists on disk (the sandbox can't
// delete files via the file tool; only the user can run `git rm`).
namespace AssetHub.Api.Infrastructure;
